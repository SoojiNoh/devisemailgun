<p id="notice"><%= notice %></p>

<h1>Listing Forgotmails</h1>

<h4>보낸 메일함</h4>
<table>
  <thead>
    <tr>
      <th>받는 이</th>
      <th>보내는 이</th>
      <th>제목</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @forgotmails.each do |forgotmail| %>
      <tr>
        <td><%=forgotmail.reciever%></td>
        <td><%=forgotmail.sender%></td>
        <td><%=forgotmail.title%></td>
        <td><%= link_to 'Show', forgotmail %></td>
        <td><%= link_to 'Edit', edit_forgotmail_path(forgotmail) %></td>
        <td><%= link_to 'Destroy', forgotmail, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '새로운 메일 보내기', new_forgotmail_path, class:  "btn btn-primary" %>
